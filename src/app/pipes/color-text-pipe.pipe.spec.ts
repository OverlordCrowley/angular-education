import { ColorTextPipePipe } from './color-text-pipe.pipe';

describe('ColorTextPipePipe', () => {
  it('create an instance', () => {
    const pipe = new ColorTextPipePipe();
    expect(pipe).toBeTruthy();
  });
});
